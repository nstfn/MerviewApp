--- a/js/renderer.js
+++ b/js/renderer.js
@@ -1053,7 +1053,7 @@
     wrapper.querySelectorAll('.mermaid-expand-btn[data-expand-target]').forEach(btn => {
         btn.addEventListener('click', () => expandMermaid(btn.dataset.expandTarget));
     });
     wrapper.querySelectorAll('.mermaid[id]').forEach(el => {
-        el.addEventListener('dblclick', () => expandMermaid(el.id));
+        el.addEventListener('click', () => expandMermaid(el.id));
     });
 }
--- a/js/mermaid-fullscreen.js
+++ b/js/mermaid-fullscreen.js
@@ -8,8 +8,6 @@
 import { state } from './state.js';

 // Fullscreen overlay background colors
-// Using 0.98 opacity (not 1.0) to allow subtle content hints behind the overlay
-// while still providing enough contrast for diagram readability
-const FULLSCREEN_BG_DARK = 'rgba(30, 30, 30, 0.98)';
-const FULLSCREEN_BG_LIGHT = 'rgba(255, 255, 255, 0.98)';
+const FULLSCREEN_BG_DARK = 'rgba(30, 30, 30, 1)';
+const FULLSCREEN_BG_LIGHT = 'rgba(255, 255, 255, 1)';

